---
description: 代码修改后自动重启前后端开发服务
alwaysApply: true
---

# 自动重启前后端

- 只要本次会话对项目代码或运行配置做了实际修改，在结束前都要自动重启前后端开发服务。
- 纯阅读、纯解释、纯文档更新、仅改 `CHANGELOG.md` / `.cursor/rules/*` 时，不需要重启。
- 重启前必须先检查现有终端状态，优先复用已有前后端终端，避免重复启动多个相同服务实例。
- 后端优先使用仓库根目录命令：`npm run dev:server`
- 前端优先使用仓库根目录命令：`API_PROXY_TARGET=http://127.0.0.1:3002 npm run dev --prefix client -- --host 127.0.0.1 --port 5175`
- 如果已有对应服务在运行，先停止旧进程，再在同类终端里重新启动；如果没有运行中的服务，再新启动。
- 重启后要检查服务是否进入稳定状态；若启动失败，需要继续排查或在最终回复中明确说明失败原因。
- 除非用户明确要求不要重启，否则不要跳过这一步。
